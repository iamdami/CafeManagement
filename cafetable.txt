CREATE TABLE SHOP(

SHOP_ID NUMBER(8) NOT NULL,
SHOP_NAME VARCHAR2(20) NOT NULL,
STAFF_ID NUMBER(8) NOT NULL,
CUSTOMER_ID NUMBER(8) NOT NULL,


);

ALTER TABLE SHOP
ADD CONSTRAINT PK_SHOP PRIMARY KEY(SHOP_ID);

ALTER TABLE SHOP
ADD CONSTRAINT FK_STAFF_ID FOREIGN KEY(STAFF_ID)
REFERENCES STAFF(STAFF_ID);

ALTER TABLE SHOP
ADD CONSTRAINT FK_CUSTOMER_ID FOREIGN KEY(CUSTOMER_ID)
REFERENCES CUSTOMER(CUSTOMER_ID);



CREATE TABLE STAFF(

STAFF_ID NUMBER(8) NOT NULL,
STAFF_NAME NVARCHAR2(20) NOT NULL,
STAFF_POSITION NVARCHAR2(20) NOT NULL,
HIREDATE DATE,
STAFF_PHONE NUMBER(12) NOT NULL

);

ALTER TABLE STAFF
ADD CONSTRAINT PK_STAFF PRIMARY KEY(STAFF_ID);



CREATE TABLE CUSTOMER(
CUSTOMER_ID NUMBER(8) NOT NULL,
CUSTOMER_NAME NVARCHAR2(20) NOT NULL,
CUSTOMER_POSITION VARCHAR2(10) NOT NULL,
LASTVISIT DATE,
CUSTOMER_PHONE NUMBER(12) NOT NULL

);

ALTER TABLE CUSTOMER
ADD CONSTRAINT PK_CUSTOMER PRIMARY KEY(CUSTOMER_ID);



CREATE TABLE MENU(
MENU_ID NUMBER(8) NOT NULL,
MENU_NAME NVARCHAR2(40) NOT NULL,
CUSTOMER_ID NUMBER(8) NOT NULL,
CALORIE NUMBER(10) NOT NULL,
PRICE NUMBER(20) NOT NULL,
SALESVOLUME NUMBER(10),
STOCKVOLUME NUMBER(10)

);

ALTER TABLE MENU
ADD CONSTRAINT PK_MENU PRIMARY KEY(MENU_ID);


ALTER TABLE MENU
ADD CONSTRAINT FK_CUSTOMER__ID FOREIGN KEY(CUSTOMER_ID)
REFERENCES CUSTOMER(CUSTOMER_ID);



CREATE TABLE ORDERLIST(
ORDER_ID NUMBER(8) NOT NULL,
STAFF_ID NUMBER(8) NOT NULL,
CUSTOMER_ID NUMBER(8) NOT NULL,
MENU_ID NUMBER(8) NOT NULL,
ORDERDATE DATE,
TOTALVOLUME NUMBER(10),
TOTALPRICE NUMBER(20) NOT NULL

);

ALTER TABLE ORDERLIST
ADD CONSTRAINT PK_ORDERLIST PRIMARY KEY(ORDER_ID);


ALTER TABLE ORDERLIST
ADD CONSTRAINT FK_STAFF__ID FOREIGN KEY(STAFF_ID)
REFERENCES STAFF(STAFF_ID);

ALTER TABLE ORDERLIST
ADD CONSTRAINT FK_CUSTOMER___ID FOREIGN KEY(CUSTOMER_ID)
REFERENCES CUSTOMER(CUSTOMER_ID);

ALTER TABLE ORDERLIST
ADD CONSTRAINT FK_MENU_ID FOREIGN KEY(MENU_ID)
REFERENCES MENU(MENU_ID);



CONSTRAINT PK_SHOP PRIMARY KEY(SHOP_ID) ,

CONSTRAINT FK_SHOP FOREIGN KEY(STAFF_ID)
REFERENCES STAFF(STAFF_ID) ,

CONSTRAINT FK_SHOP FOREIGN KEY(CUSTOMER_ID)
REFERENCES CUSTOMER(CUSTOMER_ID)